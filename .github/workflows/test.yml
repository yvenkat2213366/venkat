name: Inputs and Outputs Lab
on:
  push:
   tags:
    - "v*"
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v3

    - name: run my script
      run: python test.py
